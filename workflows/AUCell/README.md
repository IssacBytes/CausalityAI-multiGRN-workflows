# AUCell workflows

This folder is reserved for AUCell learning and reproducibility workflows.

Planned contents:

- Official package call examples.
- Parameter-focused workflow for `aucMaxRank` and `normAUC`.
- Comparison outputs aligned with the UCell workflow tables.

The official AUCell source is tracked separately as the root-level `AUCell` submodule.
